import React from 'react';
import { BaseComponent, SectionContainer, SectionTitle } from './BaseComponent';

const ExperienceTradingFloor = () => {
    return (
        <BaseComponent title="Experience Trading Floor">
            <SectionContainer>
                <SectionTitle>LBG Data Engineering Desk</SectionTitle>
                {/* Add content for current role */}
            </SectionContainer>

            <SectionContainer>
                <SectionTitle>LBG Cloud Infrastructure Division</SectionTitle>
                {/* Add content for internship */}
            </SectionContainer>
        </BaseComponent>
    );
};

export default ExperienceTradingFloor;